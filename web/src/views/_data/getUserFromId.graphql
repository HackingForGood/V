query getUserFromId ($id: String) {
    user (id: $id) {
        fullName
        email
        subjectConnection {
            edges {
                node {
                    name
                }
            }
        }
    }
}