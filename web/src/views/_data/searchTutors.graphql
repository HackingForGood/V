query searchTutors ($query: String){
    subject (query: $query) {
        users {
            fullName
            email
            baseRate
            email
            feedbackReceived {
                body
                rating
            }
            subjects {
                name
            }
        }
    }
}